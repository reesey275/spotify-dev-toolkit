name: "CodeQL Security Analysis"

on:
  push:
    branches: [ "main", "master", "docker-wsl-production-stack" ]
  pull_request:
    branches: [ "main", "master" ]
  schedule:
    # Run weekly on Mondays at 6:00 UTC
    - cron: '0 6 * * 1'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: [self-hosted, spotifydev]
    timeout-minutes: 360
    permissions:
      # Required for all workflows
      security-events: write
      # Required for workflows in private repositories
      contents: read
      actions: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: javascript-typescript
          build-mode: none
        - language: python
          build-mode: none

    steps:
    - name: Checkout repository
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.2.2

    - name: Initialize CodeQL
      uses: github/codeql-action/init@901f0fdebfadabaa0664a07bd572cbd2ff90b07e # v3.27.0
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # Enhanced security queries (more thorough, may have false positives)
        queries: +security-extended,security-and-quality

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@901f0fdebfadabaa0664a07bd572cbd2ff90b07e # v3.27.0
      with:
        category: "/language:${{ matrix.language }}"
