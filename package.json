{
  "name": "spotify-fan-website",
  "version": "1.0.0",
  "description": "Spotify Fan Website - Browse and explore Spotify playlists with dynamic features",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon",
    "dev:server": "nodemon server.js",
    "kill-port": "lsof -ti:5500 | xargs kill -9 2>/dev/null || true",
    "clean-start": "npm run kill-port && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "query-string": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
