# ============================================================================
# Environment Configuration Template - Spotify Dev Toolkit
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to `.env` in your working directory
# 2. Fill in your actual API keys and configuration values
# 3. Never commit `.env` to version control
# 4. For CI/deployment, use environment variables or a secure vault
#
# DOCUMENTATION:
# - https://developer.spotify.com/documentation/web-api
# - https://logfire.pydantic.dev/docs
#

# ============================================================================
# SPOTIFY API CREDENTIALS
# ============================================================================

# Spotify OAuth Client ID
# Register at: https://developer.spotify.com/dashboard/applications
# This is your app's public identifier
SPOTIFY_CLIENT_ID=your_client_id_here

# Spotify OAuth Client Secret
# Keep this secret! Never commit or expose it.
# Get it from: https://developer.spotify.com/dashboard/applications
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI
# Must match exactly what's registered in your Spotify app settings
# For local development, typically http://localhost:5500/callback
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5500/callback

# Optional: Spotify Username for fallback/demo flows
# Used when OAuth is not available or for default playlist fetching
SPOTIFY_USERNAME=your_spotify_username

# Optional: Specify a Spotify User ID for demo playlists
# If not provided, uses Spotify's official/curated playlists
SPOTIFY_USER_ID=your_spotify_user_id

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# Node.js environment
# Options: development, production, staging
# Affects logging, error handling, and performance optimizations
NODE_ENV=development

# Server Port
# The port the Express server will listen on
# In Docker or behind a proxy, this is often 3000 or 8080
PORT=5500

# ============================================================================
# SECURITY & SESSIONS
# ============================================================================

# Session Secret (for signing session cookies)
# Generate a strong random string for production
# In Node: crypto.randomBytes(32).toString('hex')
# Minimum 32 characters, strong entropy recommended
SESSION_SECRET=your_session_secret_here

# ============================================================================
# DOCKER CONFIGURATION
# ============================================================================

# Set to 'true' when running in a Docker container
# Affects logging, file paths, and internal configuration
DOCKER_CONTAINER=false

# User ID and Group ID for file ownership in Docker (WSL2)
# On Linux/WSL2, run: id -u (for UID) and id -g (for GID)
# Ensures generated files have correct ownership
APP_UID=1000
APP_GID=1000

# ============================================================================
# OBSERVABILITY & MONITORING (Optional)
# ============================================================================

# Logfire API Key for structured logging and tracing
# Get it from: https://logfire.pydantic.dev/dashboard
# For local development, this is optional; set only if using Logfire
LOGFIRE_API_KEY=your_logfire_api_key_here

# ============================================================================
# CLOUDFLARE TUNNEL (Optional, for public URL tunneling)
# ============================================================================

# Cloudflare Tunnel Token
# Get it from: https://dash.cloudflare.com/profile/api-tokens
# Use only if exposing the app via Cloudflare Tunnel
# CLOUDFLARE_TOKEN=your_cloudflare_token_here
